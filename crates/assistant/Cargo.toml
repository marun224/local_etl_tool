[package]
name = "etl-assistant"
description = "A local model writes pipeline documents, constrained by the manifest's schema."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
etl-metadata.workspace = true
serde_json.workspace = true
ureq = "~3.2.1"

[dev-dependencies]
etl-duckdb-engine.workspace = true
jsonschema = { version = "0.57", default-features = false }
