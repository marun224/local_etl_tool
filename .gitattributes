# AWS's SigV4 test cases are compared byte for byte: a checkout must not turn
# their LF line endings into CRLF (Windows runners check out with autocrlf).
crates/connectors/tests/fixtures/sigv4/** -text
